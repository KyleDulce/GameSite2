# Testing application to test components

### making components

`npx nx run tester:generate-component --name={name} --directory={directory} --selector={selector}`

- directory is relative to `app\tester\src\app`
